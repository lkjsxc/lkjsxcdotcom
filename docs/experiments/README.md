# Experiment Registry

[architecture-program.md](architecture-program.md) owns candidate definitions,
correctness gates, measurement protocol, and adoption criteria. Each candidate
gets a committed decision-grade implementation, exact SHA, command output
summary, result, and rejection/adoption rationale. Rejected code does not remain
behind flags on `main`.
